apply plugin: "war"

bootWar {
    mainClassName = "com.tablesa.monitoreo.MonitoreoApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
